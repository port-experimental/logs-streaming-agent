# Port Kafka Consumer Configuration
# Copy this file to .env and fill in your actual credentials

# ============================================================================
# Port API Credentials
# ============================================================================
# Get these from: Port UI → Settings → Developers → Create API Credentials
PORT_CLIENT_ID=your_port_client_id_here
PORT_CLIENT_SECRET=your_port_client_secret_here

# Your Port organization ID (provided by Port)
PORT_ORG_ID=org_your_org_id_here

# ============================================================================
# Kafka Configuration
# ============================================================================
# Kafka broker addresses (comma-separated, provided by Port)
KAFKA_BROKERS=broker1.kafka.port.io:9092,broker2.kafka.port.io:9092,broker3.kafka.port.io:9092

# Kafka authentication credentials (provided by Port)
KAFKA_USERNAME=your_kafka_username
KAFKA_PASSWORD=your_kafka_password

# Consumer group ID - MUST start with your ORG_ID or match your Port username
# Format: ORG_ID.your-consumer-name
KAFKA_CONSUMER_GROUP_ID=org_your_org_id_here.my-consumer-group

# ============================================================================
# Optional Configuration
# ============================================================================
# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Enable/disable changes topic consumption (true/false)
CONSUME_CHANGES_TOPIC=false

# ============================================================================
# Notes
# ============================================================================
# 1. Never commit this file with real credentials to version control
# 2. Kafka topics are automatically named:
#    - Actions: {PORT_ORG_ID}.runs
#    - Changes: {PORT_ORG_ID}.change.log
# 3. Consumer group ID must follow Port's naming convention
# 4. Kafka uses SCRAM-SHA-512 authentication over SSL/TLS
